function textRouter(state){
    if(state.registered == false){
        //Send registration link
    }
    else if(state.processed){
        if(state.activeCampaign){
            
        }
    }else{
        //Send "Unrecognised keyword or command, please try again. Text HELP for more info."
    }
}